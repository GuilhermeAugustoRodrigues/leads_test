{
  "name": "app",
  "version": "1.0.0",
  "description": "## About",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write '**/*' --ignore-path .gitignore"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
